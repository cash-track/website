export default defineAppConfig({
    ui: {
        colors: {
            // `cash` shades come from the @theme block in assets/css/main.css.
            primary: 'cash',
            neutral: 'gray'
        }
    }
})
